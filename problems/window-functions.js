// Window Functions — 25 problems
export const windowFunctions = [
    {
        id: 106,
        title: "Row Number",
        category: "Window Functions",
        difficulty: "easy",
        description: "Write a query to assign a row number to each employee ordered by salary (highest first).\n\nTable: employees",
        hint: "Use ROW_NUMBER() OVER (ORDER BY salary DESC).",
        solution: "SELECT name, salary, ROW_NUMBER() OVER (ORDER BY salary DESC) AS row_num FROM employees;",
    },
    {
        id: 107,
        title: "Rank by Salary",
        category: "Window Functions",
        difficulty: "easy",
        description: "[LeetCode 178] Write a query to rank employees by salary. If two employees have the same salary, they should get the same rank with no gaps.\n\nTable: employees",
        hint: "Use DENSE_RANK() OVER (ORDER BY salary DESC).",
        solution: "SELECT name, salary, DENSE_RANK() OVER (ORDER BY salary DESC) AS rank FROM employees;",
    },
    {
        id: 108,
        title: "Rank vs Dense Rank",
        category: "Window Functions",
        difficulty: "medium",
        description: "Write a query showing name, salary, RANK(), DENSE_RANK(), and ROW_NUMBER() for all employees ordered by salary desc. Observe the difference.\n\nTable: employees",
        hint: "Use all three ranking functions in one query.",
        solution: "SELECT name, salary, RANK() OVER (ORDER BY salary DESC) AS rank, DENSE_RANK() OVER (ORDER BY salary DESC) AS dense_rank, ROW_NUMBER() OVER (ORDER BY salary DESC) AS row_num FROM employees;",
    },
    {
        id: 109,
        title: "Department-wise Rank",
        category: "Window Functions",
        difficulty: "medium",
        description: "Write a query to rank employees within each department by salary (highest first).\n\nTable: employees",
        hint: "Use PARTITION BY department_id in your OVER clause.",
        solution: "SELECT name, department_id, salary, RANK() OVER (PARTITION BY department_id ORDER BY salary DESC) AS dept_rank FROM employees;",
    },
    {
        id: 110,
        title: "Top 2 Earners Per Department",
        category: "Window Functions",
        difficulty: "medium",
        description: "Write a query to find the top 2 earners in each department.\n\nTable: employees",
        hint: "Use a CTE with DENSE_RANK PARTITION BY dept, then filter rank <= 2.",
        solution: "WITH ranked AS (SELECT name, department_id, salary, DENSE_RANK() OVER (PARTITION BY department_id ORDER BY salary DESC) AS rnk FROM employees) SELECT * FROM ranked WHERE rnk <= 2;",
    },
    {
        id: 111,
        title: "Running Total of Orders",
        category: "Window Functions",
        difficulty: "medium",
        description: "Write a query to show each order with a running total of the amount, ordered by date.\n\nTable: orders",
        hint: "SUM(amount) OVER (ORDER BY order_date).",
        solution: "SELECT id, order_date, amount, SUM(amount) OVER (ORDER BY order_date) AS running_total FROM orders WHERE status = 'completed';",
    },
    {
        id: 112,
        title: "Running Total Per Customer",
        category: "Window Functions",
        difficulty: "medium",
        description: "Write a query to show a running total of spending for each customer separately.\n\nTable: orders",
        hint: "Use PARTITION BY customer_id in SUM OVER.",
        solution: "SELECT customer_id, order_date, amount, SUM(amount) OVER (PARTITION BY customer_id ORDER BY order_date) AS customer_running_total FROM orders WHERE status = 'completed';",
    },
    {
        id: 113,
        title: "LAG — Previous Order Amount",
        category: "Window Functions",
        difficulty: "medium",
        description: "Write a query to show each order alongside the previous order's amount (for the same customer).\n\nTable: orders",
        hint: "Use LAG(amount) OVER (PARTITION BY customer_id ORDER BY order_date).",
        solution: "SELECT customer_id, order_date, amount, LAG(amount) OVER (PARTITION BY customer_id ORDER BY order_date) AS prev_amount FROM orders WHERE status = 'completed';",
    },
    {
        id: 114,
        title: "LEAD — Next Order Amount",
        category: "Window Functions",
        difficulty: "medium",
        description: "Write a query to show each order alongside the next order's amount (same customer).\n\nTable: orders",
        hint: "Use LEAD(amount) OVER (PARTITION BY customer_id ORDER BY order_date).",
        solution: "SELECT customer_id, order_date, amount, LEAD(amount) OVER (PARTITION BY customer_id ORDER BY order_date) AS next_amount FROM orders WHERE status = 'completed';",
    },
    {
        id: 115,
        title: "Salary Difference from Previous",
        category: "Window Functions",
        difficulty: "medium",
        description: "For each employee's salary history, show the salary increase from the previous period.\n\nTable: salaries",
        hint: "Use LAG then subtract.",
        solution: "SELECT emp_id, from_date, salary, salary - LAG(salary) OVER (PARTITION BY emp_id ORDER BY from_date) AS salary_increase FROM salaries;",
    },
    {
        id: 116,
        title: "Moving Average (3-Day)",
        category: "Window Functions",
        difficulty: "hard",
        description: "Write a query to calculate a 3-day moving average of temperature for New York.\n\nTable: weather",
        hint: "Use AVG with ROWS BETWEEN 2 PRECEDING AND CURRENT ROW.",
        solution: "SELECT date, temperature, ROUND(AVG(temperature) OVER (ORDER BY date ROWS BETWEEN 2 PRECEDING AND CURRENT ROW), 2) AS moving_avg_3day FROM weather WHERE city = 'New York';",
    },
    {
        id: 117,
        title: "Percentage of Total",
        category: "Window Functions",
        difficulty: "medium",
        description: "Write a query to show each employee's salary as a percentage of the total salary.\n\nTable: employees",
        hint: "salary * 100.0 / SUM(salary) OVER ().",
        solution: "SELECT name, salary, ROUND(salary * 100.0 / SUM(salary) OVER (), 2) AS salary_pct FROM employees ORDER BY salary_pct DESC;",
    },
    {
        id: 118,
        title: "Percentage Within Department",
        category: "Window Functions",
        difficulty: "medium",
        description: "Write a query to show each employee's salary as a percentage of their department's total salary.\n\nTable: employees",
        hint: "PARTITION BY department_id in SUM OVER.",
        solution: "SELECT name, department_id, salary, ROUND(salary * 100.0 / SUM(salary) OVER (PARTITION BY department_id), 2) AS dept_salary_pct FROM employees;",
    },
    {
        id: 119,
        title: "NTILE — Quartiles",
        category: "Window Functions",
        difficulty: "medium",
        description: "Write a query to divide employees into 4 salary quartiles (1 = lowest, 4 = highest).\n\nTable: employees",
        hint: "Use NTILE(4) OVER (ORDER BY salary).",
        solution: "SELECT name, salary, NTILE(4) OVER (ORDER BY salary) AS quartile FROM employees;",
    },
    {
        id: 120,
        title: "First and Last Value",
        category: "Window Functions",
        difficulty: "medium",
        description: "For each department, show every employee alongside the highest and lowest earner's name in their department.\n\nTable: employees",
        hint: "Use FIRST_VALUE and LAST_VALUE with proper frame.",
        solution: "SELECT name, department_id, salary, FIRST_VALUE(name) OVER (PARTITION BY department_id ORDER BY salary DESC) AS highest_earner, FIRST_VALUE(name) OVER (PARTITION BY department_id ORDER BY salary ASC) AS lowest_earner FROM employees;",
    },
    {
        id: 121,
        title: "Cumulative Count",
        category: "Window Functions",
        difficulty: "easy",
        description: "Write a query to show a cumulative count of orders by date.\n\nTable: orders",
        hint: "COUNT(*) OVER (ORDER BY order_date).",
        solution: "SELECT order_date, amount, COUNT(*) OVER (ORDER BY order_date) AS cumulative_orders FROM orders WHERE status = 'completed';",
    },
    {
        id: 122,
        title: "Top N Per Group",
        category: "Window Functions",
        difficulty: "hard",
        description: "[LeetCode 185] Write a query to find the top 3 earners in each department. Show department name, employee name, salary.\n\nTables: employees, departments",
        hint: "CTE with DENSE_RANK PARTITION BY dept, filter rnk <= 3.",
        solution: "WITH ranked AS (SELECT e.name, d.name AS department, e.salary, DENSE_RANK() OVER (PARTITION BY e.department_id ORDER BY e.salary DESC) AS rnk FROM employees e JOIN departments d ON e.department_id = d.id) SELECT department, name, salary FROM ranked WHERE rnk <= 3;",
    },
    {
        id: 123,
        title: "Difference from Department Average",
        category: "Window Functions",
        difficulty: "medium",
        description: "Write a query to show each employee's salary and how much it differs from their department average.\n\nTable: employees",
        hint: "salary - AVG(salary) OVER (PARTITION BY department_id).",
        solution: "SELECT name, department_id, salary, ROUND(salary - AVG(salary) OVER (PARTITION BY department_id), 2) AS diff_from_dept_avg FROM employees;",
    },
    {
        id: 124,
        title: "Order Gap Analysis",
        category: "Window Functions",
        difficulty: "hard",
        description: "For each customer, show the number of days between consecutive orders.\n\nTable: orders",
        hint: "Use LAG to get previous order date, then calculate difference.",
        solution: "SELECT customer_id, order_date, LAG(order_date) OVER (PARTITION BY customer_id ORDER BY order_date) AS prev_order_date, CAST(julianday(order_date) - julianday(LAG(order_date) OVER (PARTITION BY customer_id ORDER BY order_date)) AS INTEGER) AS days_gap FROM orders WHERE status = 'completed';",
    },
    {
        id: 125,
        title: "Median Salary",
        category: "Window Functions",
        difficulty: "hard",
        description: "Write a query to find the median salary of all employees.\n\nTable: employees\n\nHint: Median is the middle value when sorted.",
        hint: "Use ROW_NUMBER and COUNT, then pick the middle row(s).",
        solution: "WITH numbered AS (SELECT salary, ROW_NUMBER() OVER (ORDER BY salary) AS rn, COUNT(*) OVER () AS total FROM employees) SELECT AVG(salary) AS median_salary FROM numbered WHERE rn IN (total / 2, total / 2 + 1);",
    },
    {
        id: 126,
        title: "Year-over-Year Comparison",
        category: "Window Functions",
        difficulty: "hard",
        description: "For each employee with salary history, show current salary, previous salary, and the percentage increase.\n\nTable: salaries",
        hint: "Use LAG to get previous salary, then calculate percentage.",
        solution: "SELECT emp_id, from_date, salary, LAG(salary) OVER (PARTITION BY emp_id ORDER BY from_date) AS prev_salary, ROUND((salary - LAG(salary) OVER (PARTITION BY emp_id ORDER BY from_date)) * 100.0 / LAG(salary) OVER (PARTITION BY emp_id ORDER BY from_date), 2) AS pct_increase FROM salaries;",
    },
    {
        id: 127,
        title: "Dense Rank with Ties",
        category: "Window Functions",
        difficulty: "medium",
        description: "Write a query to rank products by price. Products with the same price should have the same rank. Show product name, price, and rank.\n\nTable: products",
        hint: "DENSE_RANK() OVER (ORDER BY price DESC).",
        solution: "SELECT name, price, DENSE_RANK() OVER (ORDER BY price DESC) AS price_rank FROM products;",
    },
    {
        id: 128,
        title: "Partition By Multiple Columns",
        category: "Window Functions",
        difficulty: "medium",
        description: "Write a query to rank students by score within each grade and department combination.\n\nTable: students",
        hint: "PARTITION BY grade, department.",
        solution: "SELECT name, grade, department, score, RANK() OVER (PARTITION BY grade, department ORDER BY score DESC) AS rank_in_group FROM students;",
    },
    {
        id: 129,
        title: "Consecutive Salary Increases",
        category: "Window Functions",
        difficulty: "hard",
        description: "Find employees who have had salary increases in every period (never a decrease).\n\nTable: salaries",
        hint: "Use LAG to get previous salary, check all differences are positive.",
        solution: "WITH salary_changes AS (SELECT emp_id, salary, LAG(salary) OVER (PARTITION BY emp_id ORDER BY from_date) AS prev_salary FROM salaries) SELECT DISTINCT emp_id FROM salary_changes WHERE emp_id NOT IN (SELECT emp_id FROM salary_changes WHERE prev_salary IS NOT NULL AND salary <= prev_salary);",
    },
    {
        id: 130,
        title: "Running Max and Min",
        category: "Window Functions",
        difficulty: "medium",
        description: "Write a query to show the running maximum and minimum temperature for New York.\n\nTable: weather",
        hint: "MAX/MIN OVER (ORDER BY date).",
        solution: "SELECT date, temperature, MAX(temperature) OVER (ORDER BY date) AS running_max, MIN(temperature) OVER (ORDER BY date) AS running_min FROM weather WHERE city = 'New York';",
    },
];
