// Aggregate Functions — 15 FAANG Interview Questions
export const aggregates = [
    {
        id: 31,
        title: "Average Salary",
        category: "Aggregates",
        difficulty: "easy",
        description: "[Amazon] Write a query to find the average salary across all employees.\n\nTable: employees\n\nAVG() is the most common aggregate function tested in interviews.",
        hint: "Use AVG(salary) to compute the mean.",
        solution: "SELECT ROUND(AVG(salary), 2) AS avg_salary FROM employees;",
    },
    {
        id: 32,
        title: "Min and Max Salary",
        category: "Aggregates",
        difficulty: "easy",
        description: "[Google] Write a query to find the minimum and maximum salary among all employees.\n\nTable: employees\n\nMIN/MAX are basic but frequently asked — interviewers often combine them in one query.",
        hint: "Use MIN() and MAX() together in a single SELECT.",
        solution: "SELECT MIN(salary) AS min_salary, MAX(salary) AS max_salary FROM employees;",
    },
    {
        id: 33,
        title: "Total Revenue from Completed Orders",
        category: "Aggregates",
        difficulty: "easy",
        description: "[Amazon · Flipkart] Write a query to find the total revenue from all completed orders.\n\nTable: orders\n\nSUM() with a WHERE filter is a classic e-commerce analytics question.",
        hint: "Use SUM(amount) with WHERE status = 'completed'.",
        solution: "SELECT SUM(amount) AS total_revenue FROM orders WHERE status = 'completed';",
    },
    {
        id: 34,
        title: "Second Highest Salary (NULL-safe)",
        category: "Aggregates",
        difficulty: "medium",
        description: "[LeetCode 176 · Amazon · Google] Write a query to get the second highest salary. If there is no second highest salary, return NULL.\n\nTable: employees\n\nThis is the #1 most asked SQL aggregate question at FAANG. The NULL-safe version uses IFNULL or subquery wrapping.",
        hint: "Use a subquery: SELECT MAX(salary) WHERE salary < (SELECT MAX(salary) FROM employees). Or wrap LIMIT/OFFSET in a subquery.",
        solution: "SELECT (SELECT DISTINCT salary FROM employees ORDER BY salary DESC LIMIT 1 OFFSET 1) AS SecondHighestSalary;",
    },
    {
        id: 35,
        title: "Count Products by Category",
        category: "Aggregates",
        difficulty: "easy",
        description: "[Uber · Meta] Write a query to count the number of products in each category.\n\nTable: products\n\nCOUNT() + GROUP BY is the bread and butter of data analytics interviews.",
        hint: "Use COUNT(*) with GROUP BY category.",
        solution: "SELECT category, COUNT(*) AS product_count FROM products GROUP BY category;",
    },
    {
        id: 36,
        title: "Department Highest Salary",
        category: "Aggregates",
        difficulty: "medium",
        description: "[LeetCode 184 · Meta · Amazon] Write a query to find the employee(s) with the highest salary in each department. Show department name, employee name, and salary.\n\nTables: employees, departments\n\nThis is a top FAANG question — tests correlated subqueries with aggregates.",
        hint: "Use a subquery to find MAX(salary) per department, then JOIN back to get the employee details.",
        solution: "SELECT d.name AS Department, e.name AS Employee, e.salary AS Salary FROM employees e JOIN departments d ON e.department_id = d.id WHERE e.salary = (SELECT MAX(salary) FROM employees e2 WHERE e2.department_id = e.department_id);",
    },
    {
        id: 37,
        title: "Total Salary per Department",
        category: "Aggregates",
        difficulty: "easy",
        description: "[Microsoft] Write a query to find the total salary expense for each department. Show department_id and total.\n\nTable: employees\n\nSUM() with GROUP BY — a fundamental analytics query asked everywhere.",
        hint: "Use SUM(salary) with GROUP BY department_id.",
        solution: "SELECT department_id, SUM(salary) AS total_salary FROM employees GROUP BY department_id;",
    },
    {
        id: 38,
        title: "Orders Per Customer",
        category: "Aggregates",
        difficulty: "easy",
        description: "[Amazon · Walmart] Write a query to find how many orders each customer has placed. Show customer_id and order count, sorted by most orders first.\n\nTable: orders\n\nCounting per-entity is the most common real-world analytics task.",
        hint: "Use COUNT(*) with GROUP BY customer_id, ORDER BY count DESC.",
        solution: "SELECT customer_id, COUNT(*) AS order_count FROM orders GROUP BY customer_id ORDER BY order_count DESC;",
    },
    {
        id: 39,
        title: "Revenue by Product Category",
        category: "Aggregates",
        difficulty: "medium",
        description: "[Google · Amazon] Write a query to find the total revenue generated by each product category from completed orders.\n\nTables: orders, products\n\nJOIN + aggregate is a common multi-table analytics question at FAANG.",
        hint: "JOIN orders with products, filter completed, then GROUP BY category with SUM(amount).",
        solution: "SELECT p.category, SUM(o.amount) AS total_revenue FROM orders o JOIN products p ON o.product_id = p.id WHERE o.status = 'completed' GROUP BY p.category ORDER BY total_revenue DESC;",
    },
    {
        id: 40,
        title: "Average Order Value",
        category: "Aggregates",
        difficulty: "easy",
        description: "[Uber · Swiggy] Write a query to find the average order value for completed orders, rounded to 2 decimal places.\n\nTable: orders\n\nAOV is a key business metric — frequently asked in e-commerce and delivery company interviews.",
        hint: "Use ROUND(AVG(amount), 2) with a WHERE filter for completed orders.",
        solution: "SELECT ROUND(AVG(amount), 2) AS avg_order_value FROM orders WHERE status = 'completed';",
    },
    {
        id: 41,
        title: "Employees Per City",
        category: "Aggregates",
        difficulty: "easy",
        description: "[Meta · LinkedIn] Write a query to count employees in each city, ordered by headcount descending.\n\nTable: employees\n\nGrouping by location is a common HR analytics question.",
        hint: "COUNT(*) with GROUP BY city, ORDER BY count DESC.",
        solution: "SELECT city, COUNT(*) AS emp_count FROM employees GROUP BY city ORDER BY emp_count DESC;",
    },
    {
        id: 42,
        title: "Salary Range Per Department",
        category: "Aggregates",
        difficulty: "medium",
        description: "[Goldman Sachs · Microsoft] Write a query to find the salary spread (max minus min) for each department.\n\nTable: employees\n\nSalary range analysis is common in finance and HR analytics interviews.",
        hint: "Use MAX(salary) - MIN(salary) with GROUP BY department_id.",
        solution: "SELECT department_id, MAX(salary) - MIN(salary) AS salary_range FROM employees GROUP BY department_id;",
    },
    {
        id: 43,
        title: "Monthly Revenue in 2023",
        category: "Aggregates",
        difficulty: "medium",
        description: "[Amazon · Google] Write a query to compute total revenue for each month in 2023 from completed orders. Show month number and total.\n\nTable: orders\n\nDate extraction + aggregation is a top analytics question — tests strftime/EXTRACT knowledge.",
        hint: "Use strftime('%m', order_date) to extract month in SQLite, then GROUP BY.",
        solution: "SELECT strftime('%m', order_date) AS month, SUM(amount) AS monthly_revenue FROM orders WHERE strftime('%Y', order_date) = '2023' AND status = 'completed' GROUP BY month ORDER BY month;",
    },
    {
        id: 44,
        title: "Count Distinct Countries",
        category: "Aggregates",
        difficulty: "easy",
        description: "[Uber · Airbnb] Write a query to count the number of unique countries from the customers table.\n\nTable: customers\n\nCOUNT(DISTINCT ...) is a subtle but important concept — tests if you know the difference from COUNT(*).",
        hint: "Use COUNT(DISTINCT country).",
        solution: "SELECT COUNT(DISTINCT country) AS country_count FROM customers;",
    },
    {
        id: 45,
        title: "Customer Lifetime Value",
        category: "Aggregates",
        difficulty: "medium",
        description: "[Amazon · Netflix] Write a query to find each customer's total spending and number of orders (completed only). Show customer name, total_spent, and order_count. Sort by total_spent descending.\n\nTables: customers, orders\n\nCLV (Customer Lifetime Value) is one of the most important business metrics tested at FAANG.",
        hint: "JOIN customers with orders, filter completed, GROUP BY customer, use SUM and COUNT.",
        solution: "SELECT c.name, SUM(o.amount) AS total_spent, COUNT(o.id) AS order_count FROM customers c JOIN orders o ON c.id = o.customer_id WHERE o.status = 'completed' GROUP BY c.id ORDER BY total_spent DESC;",
    },
    {
        id: 46,
        title: "Average Experience Per Department",
        category: "Aggregates",
        difficulty: "easy",
        description: "[LeetCode 1075 — Project Employees I] Write a query to find the average years of experience for employees in each department. Round to 2 decimal places.\n\nTables: employees, departments\n\nLeetCode 1075 maps to: each 'project' is a department, and 'experience_years' maps to (2024 - hire_year). JOIN + AVG is the core skill.",
        hint: "JOIN employees with departments, compute AVG of years since hire, GROUP BY department.",
        solution: "SELECT d.name AS department, ROUND(AVG(2024 - CAST(strftime('%Y', e.hire_date) AS INTEGER)), 2) AS avg_experience_years FROM employees e JOIN departments d ON e.department_id = d.id GROUP BY d.id ORDER BY avg_experience_years DESC;",
    },
    {
        id: 47,
        title: "Warehouse Manager",
        category: "Aggregates",
        difficulty: "easy",
        description: "[LeetCode 1718] Find the total volume of inventory stored in each warehouse. Volume is the SUM of units across all products in that warehouse.\n\nTable: warehouse\n\nExact LeetCode 1718 — SUM(units) GROUP BY warehouse name. Simple aggregation but tests knowing to SUM a secondary attribute.",
        hint: "GROUP BY name, SUM(units) AS total_units.",
        solution: "SELECT name AS warehouse_name, SUM(units) AS total_units FROM warehouse GROUP BY name ORDER BY warehouse_name;",
    },
    {
        id: 48,
        title: "Bank Account Summary II",
        category: "Aggregates",
        difficulty: "easy",
        description: "[LeetCode 1734] Report the name and balance of users with a balance greater than 10000. Balance is the sum of credit transactions minus debit transactions.\n\nTable: transactions\n\nLeetCode 1734 — SUM(CASE WHEN type = 'credit'...) - SUM(debit), HAVING balance > 10000.",
        hint: "SUM(CASE WHEN type='credit' THEN amount ELSE -amount END) AS balance, HAVING balance > 10000.",
        solution: "SELECT account_id, SUM(CASE WHEN type = 'credit' THEN amount ELSE -amount END) AS balance FROM transactions GROUP BY account_id HAVING SUM(CASE WHEN type = 'credit' THEN amount ELSE -amount END) > 10000 ORDER BY account_id;",
    },
    {
        id: 49,
        title: "Employees With Missing Information",
        category: "Aggregates",
        difficulty: "easy",
        description: "[LeetCode 2110] Find the IDs of all employees with missing information. An employee has missing info if they are in the employees table but not in the salaries table, OR in salaries table but not in employees table.\n\nTables: employees, salaries\n\nLeetCode 2110 — FULL OUTER JOIN equivalent using UNION of two LEFT JOINs. Detects symmetric set difference.",
        hint: "UNION: (employees without salary) UNION (salaries without employee). ORDER BY id.",
        solution: "SELECT e.id FROM employees e LEFT JOIN salaries s ON e.id = s.emp_id WHERE s.emp_id IS NULL UNION SELECT s.emp_id AS id FROM salaries s LEFT JOIN employees e ON s.emp_id = e.id WHERE e.id IS NULL ORDER BY id;",
    },
];

